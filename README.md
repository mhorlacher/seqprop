# seqprop

A short description of the project. 


## Installation

### Using Pip

```
pip install seqprop
```

or 

```
pip install git+https://github.com/github.com/mhorlacher/seqprop.git@SOME_BRANCH
```

to install from a specific branch (e.g. the latest development version of package version). 

### Using Conda

```
git clone https://github.com/github.com/mhorlacher/seqprop
cd seqprop
make env
conda activate seqprop
```

### Using Docker

First, pull the docker image. 
```
docker pull 
```

Then, run commands as shown below. 
```
docker run seqprop --help 
```
To provide input files and capture output file, mount host files and/or directories using `--mount` (see the Docker [docs](https://docs.docker.com/engine/storage/bind-mounts/)). 


## Usage

See the [documentation](https://mhorlacher.github.io/seqprop) for a usage guide and API reference. 